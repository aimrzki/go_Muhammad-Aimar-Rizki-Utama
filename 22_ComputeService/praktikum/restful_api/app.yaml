runtime: go221

runtime_config:
  operating_system: "ubuntu22"
  service: aimar-api

handlers:
  - url: /static
    static_dir: static

  - url: /.*
    script: auto

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 10

instance_class: F2
